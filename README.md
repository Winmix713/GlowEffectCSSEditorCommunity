
  # Glow Effect CSS Editor (Community)

  This is a code bundle for Glow Effect CSS Editor (Community). The original project is available at https://www.figma.com/design/Sdy0OizXjGmoJyR52Gmvuw/Glow-Effect-CSS-Editor--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  